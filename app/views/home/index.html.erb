<section class="wrapper">
  <div class="container">
    <div class="content">
      <h2 class="heading">GO TO MOUNTAINとは</h2>
      <p>気になる山の情報をシェアできるサービスです</p>
      <p>
        <%= search_form_for @q_header, url: search_mountains_path do |f| %>
        <%= f.search_field :name_cont, placeholder: "山の名前" %>
        <%= f.submit "山検索" %>
        <% end %>
       </p>
    </div>
  </div>
</section>

